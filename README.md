# React native template with Typescript and Expo

* React
* React native
* Typescript
* Expo

# Install

```
git clone git@github.com:shunsukehondo/react-native-typescript-expo-template.git your_project
cd your_project
yarn install --dev
```

# Run

Make sure Xcode installed.

```
yarn ios
```
